import styled from 'styled-components'

const LoadingWrapper = styled.div`
    display: flex;
    flex: 1;
    justify-content: center;
    align-items: center;
    padding: 200px 0px;
`

export default {
  LoadingWrapper
}
